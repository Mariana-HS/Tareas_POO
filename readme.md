# ✏️ Tarea 4 de la unidad 2

# 🚗 Clase Coche - Tarea de Programación Orientada a Objetos

### Descripción de la tarea

En esta tarea se ha implementado una clase llamada **Coche**, la cual incluye los siguientes elementos:

1. **Constructor:**  
   El constructor inicializa los atributos básicos del coche: `marca`, `modelo` y `año`.

2. **Método mostrarInformacion():**  
   Este método imprime los detalles completos de un coche, mostrando su marca, modelo y año de fabricación.

3. **Método calcularEdadDelCoche(int añoActual):**  
   Se ha añadido un método adicional para calcular la edad del coche en función del año actual, lo que permite conocer cuántos años tiene.

### Ejemplo de implementación

En el método `main` de una clase de prueba, se han creado **tres objetos** diferentes de tipo **Coche** con distintos valores para los atributos mencionados. Cada uno de estos objetos usa el método `mostrarInformacion()` para mostrar sus detalles en consola, y también se utiliza el método `calcularEdadDelCoche()` para calcular la edad de cada coche.

---

Atentamente,  
**Mariana Herrejon Silva**

